source $HOME/.work_exports
source $HOME/.zsh_aliases
source $HOME/.nvm/nvm.sh

# after upgrading openssh, brew told me to do this
eval $(ssh-agent)
function cleanup {
    echo "Killing SSH-Agent"
    kill -9 $SSH_AGENT_PID
}
trap cleanup EXIT

. /usr/local/etc/profile.d/z.sh
